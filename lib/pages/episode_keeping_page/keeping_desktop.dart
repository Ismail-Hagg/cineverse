import 'package:flutter/cupertino.dart';

class KeepingDesktop extends StatelessWidget {
  const KeepingDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Keeping Desktop'),
    );
  }
}
