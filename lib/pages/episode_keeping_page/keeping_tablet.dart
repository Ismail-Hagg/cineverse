import 'package:flutter/cupertino.dart';

class KeepingTablet extends StatelessWidget {
  const KeepingTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Keeping Tablet'),
    );
  }
}
