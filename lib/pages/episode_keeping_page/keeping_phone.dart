import 'package:flutter/cupertino.dart';

class KeepingPhone extends StatelessWidget {
  const KeepingPhone({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Keeping Phone'),
    );
  }
}
