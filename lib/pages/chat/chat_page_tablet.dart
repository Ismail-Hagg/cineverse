import 'package:flutter/material.dart';

class ChatPageTablet extends StatelessWidget {
  const ChatPageTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
