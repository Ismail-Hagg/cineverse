import 'package:flutter/material.dart';

class ChatPageDesktop extends StatelessWidget {
  const ChatPageDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
