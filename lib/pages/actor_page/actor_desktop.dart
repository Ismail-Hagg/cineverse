import 'package:flutter/cupertino.dart';

class ActorDesktop extends StatelessWidget {
  const ActorDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
