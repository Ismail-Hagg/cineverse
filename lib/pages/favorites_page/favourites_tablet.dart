import 'package:flutter/cupertino.dart';

class FavouritesTablet extends StatelessWidget {
  const FavouritesTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Favourites Tablet'),
    );
  }
}
