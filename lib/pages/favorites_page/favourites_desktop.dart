import 'package:flutter/cupertino.dart';

class FavouritesDesktop extends StatelessWidget {
  const FavouritesDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Favourites Desktop'),
    );
  }
}
