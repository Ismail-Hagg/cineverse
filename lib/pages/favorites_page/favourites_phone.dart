import 'package:flutter/cupertino.dart';

class FavouritesPhone extends StatelessWidget {
  const FavouritesPhone({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Favourites Phone'),
    );
  }
}
