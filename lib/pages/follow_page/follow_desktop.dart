import 'package:flutter/material.dart';

class FollowDesktop extends StatelessWidget {
  const FollowDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
