import 'package:flutter/material.dart';

class FollowTablet extends StatelessWidget {
  const FollowTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
