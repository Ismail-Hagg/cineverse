import 'package:flutter/cupertino.dart';

class WatchlistTablet extends StatelessWidget {
  const WatchlistTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Watchlist Tablet'),
    );
  }
}
