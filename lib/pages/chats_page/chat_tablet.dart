import 'package:flutter/cupertino.dart';

class ChatTablet extends StatelessWidget {
  const ChatTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Chat Tablet'),
    );
  }
}
