import 'package:flutter/cupertino.dart';

class ChatDesktop extends StatelessWidget {
  const ChatDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Chat Desktop'),
    );
  }
}
