import 'package:flutter/material.dart';

class InfoPhone extends StatelessWidget {
  const InfoPhone({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Phone Info'),
      ),
    );
  }
}
