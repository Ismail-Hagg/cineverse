import 'package:flutter/material.dart';

class InfoTablet extends StatelessWidget {
  const InfoTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tablet Info'),
      ),
    );
  }
}
