import 'package:flutter/material.dart';

class InfoDesktop extends StatelessWidget {
  const InfoDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Desktop Info'),
      ),
    );
  }
}
